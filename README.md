The neural network determines whether the image is photoshopped or not, and assigns a label map. The U-NET model is used for operation.


How do I install it?

Install python 3.12, but not the latest version! If it is not installed
Download the zip or clone it to your working directory.
Go to your directory and open terminal here.
Write down the python -mv command.venv
Next, write .\\.venv\Scripts\activate
Enter the pip install -r command.\requirements.txt

Make sure that your dependencies have been installed.
To run your NN, write the stream lite run command app_streamlit.py